/**
 * A feathers service to manage CRUD on MongoDB databases.
 * Pass in a connected MongoClient and it will allow you to create,
 * read, update, and delete databases on the client.
 *
 * @param  {[type]} db [description]
 * @return {[type]}    [description]
 */
module.exports = function(db) {

	var dbService = {
		find: function(params, callback) {
		  amityMongo.db.listDatabases(function(err, databases){
			  callback(null, databases.databases);
		  });
		},

		create: function(data, params, callback) {
			console.log(data);
			callback(null, data);
		},

		update: function(id, data, params, callback) {
			console.log(data);
			callback(err, data);
		},

		remove: function(id, params, callback) {
			console.log(data);
			callback(err, data);
		},

		setup: function(app) {
			console.log(app);
			this.service = app.service.bind(app);
		}
	};

	return dbService;
};
